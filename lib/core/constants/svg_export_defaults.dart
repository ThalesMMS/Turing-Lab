/// English defaults baked into SVG when the caller has no locale yet.
///
/// User-facing export paths must override these with localized strings.
const kDefaultSvgEmptyAutomatonLabel = 'No states defined';

/// Default TM legend caption (English).
const kDefaultSvgTmLegendLabel = 'δ(q, s) = (q′, w, d) — read/write/move';
